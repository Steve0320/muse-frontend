<div class="tv">
  <div class="season-cards">
    {{~#each seasonByNumber as |season|~}}
      <div class="tv-seasons">
        <h3>Season {{season.season_number}}</h3>
      </div>
    {{~/each~}}
  </div>
</div>

{{media-menu home="tv.index"}}